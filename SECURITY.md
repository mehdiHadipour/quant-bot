# Security Policy

- Never commit Telegram tokens, encryption keys, exchange credentials, or personal access tokens.
- Store runtime secrets only in GitHub Actions Secrets.
- The bot is signal-only and must not be treated as a guaranteed-profit system.
- Before enabling any future execution integration, add paper-trading and explicit kill-switch tests first.
- If a secret is exposed, revoke/rotate it immediately.
