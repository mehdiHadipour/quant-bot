# V30.7 Research & Policy

## Runtime/session policy
- London/NY overlap weight 1.00; standard threshold 70 in research backtest.
- Asia/Europe overlap weight 0.86; threshold 68.
- London weight 0.88; threshold 65.
- New York weight 0.92; threshold 74 and additional negative-session gate.
- Asia weight 0.72; threshold 78 and additional negative-session gate.
- Negative sessions: Asia and New York. They remain enabled but require OTE + MACD + directional order-flow + HLI >= 70 + score >= 80.

## Defensive symbols
The previous defensive set is retained. DOTUSDT, LINKUSDT and XRPUSDT were added because they were negative in the iterative V30.6/V30.7 in-sample reports. Defensive symbols require OTE + MACD + directional order-flow + HLI >= 70 + score >= 80.

## Packaging/runtime audit
- Root-level GitHub package (no extra wrapper directory).
- GitHub workflow uses setup-python, dependency install, validation, compile and unit tests.
- Android/Termux setup runs the same validation and tests.
- No hard-coded credentials detected by validator.
- 11 unit tests pass; Python compile passes; backtest runner passes.

## Anti-overfitting
This policy is deliberately conservative but is still in-sample. Do not infer robustness from the +16R result. Use an unseen time range and realistic fees/slippage before live trading.
