# V30.9.7 Adaptive Fully Configurable
- Added configurable regime engine: BULL/BEAR/RANGE/TRANSITION + confidence.
- Added fully configurable DST-aware sessions and overlaps.
- Added per-symbol override structure and research promotion criteria.
- Expanded manual_settings.json as the single source of truth.
- Live and unified backtest continue to call the same `analyze_market()` signal function.
- Safe default keeps regime as a non-destructive classifier (`hard_gate=false`) until out-of-sample validation proves a gate improves robustness.
